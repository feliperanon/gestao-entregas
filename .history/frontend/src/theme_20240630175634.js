// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#8B0000', // Vinho
    },
    secondary: {
      main: '#FFFAFA', // Branca para contraste
    },
  },
});

export default theme;
